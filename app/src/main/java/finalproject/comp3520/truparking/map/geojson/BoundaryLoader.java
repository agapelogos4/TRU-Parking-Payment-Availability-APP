package finalproject.comp3520.truparking.data.geojson;

import android.content.Context;
import android.util.Log;

import com.google.android.gms.maps.GoogleMap;
import com.google.maps.android.data.geojson.GeoJsonLayer;

import org.json.JSONException;

import java.io.IOException;

import finalproject.comp3520.truparking.R;
import finalproject.comp3520.truparking.utils.ReflectionUtil;

public class BoundaryLoader {

//    public static GeoJsonLayer getGeoJsonLot(String jsonFileName, GoogleMap map, Context context) {
//        int jsonFile = ReflectionUtil.getResourceIdFromString(jsonFileName.toLowerCase(), R.raw.class);
//
//        try {
//            GeoJsonLayer layer = new GeoJsonLayer(map, jsonFile, context);
//        }   catch (IOException e) {
//            Log.e("Boundary Load", this.getName() + ".json could not be found.");
//        } catch (JSONException e) {
//            Log.e("Boundary Load", this.getName() + ".json is not formatted correctly.");
//        }
//    }
}
